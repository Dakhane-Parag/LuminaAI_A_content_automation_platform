# ============================================================
# Brandflow AI - Content Automation Platform
# Backend Dependencies
# ============================================================
# NOTE: Versions chosen to have pre-built binary wheels for Python 3.13
#       on Windows — avoids requiring Rust/MSVC build tools.

# Core Framework
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Database
motor==3.7.0
pymongo==4.12.1          # SRV (Atlas) support is built-in since pymongo 4.x via dnspython

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Validation & Serialization  (pydantic v2 with Python 3.13 wheels)
pydantic==2.11.4
pydantic-settings==2.9.1
email-validator==2.2.0

# Environment & Config
python-dotenv==1.1.0

# HTTP & Utilities
python-multipart==0.0.20
httpx==0.28.1

# Security
bcrypt==4.3.0
cryptography==45.0.3

# AI / Gemini
google-generativeai==0.8.5

# AI / Replicate (Image Generation)
replicate==1.0.4

# Cloud Storage / Amazon S3
boto3==1.38.18

# Task Queue / Background Processing
celery==5.4.0
redis==5.2.1
kombu==5.4.2
